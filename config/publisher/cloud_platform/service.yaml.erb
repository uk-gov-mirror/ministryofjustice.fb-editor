apiVersion: v1
kind: Service
metadata:
  labels:
    run: <%= service_slug %>
  name: <%= service_slug %>
  namespace: <%= namespace %>
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    run: <%= service_slug %>
